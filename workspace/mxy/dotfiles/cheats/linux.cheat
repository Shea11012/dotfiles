%linux

# mount heiheihei
sudo mount -o "uid=$(id -u),gid=$(id -g),umask=022"

# switch monitor off/on dp-1
wlr-randr --output DP-1 --<switch>

# move chat to dp-1
niri msg action move-workspace-to-monitor --reference 'chat' 'DP-1'
